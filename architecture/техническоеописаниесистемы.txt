Техническое описание системы

система предназначена для быстрой разработки трехзвенных решений erp уровня для малых средних и крупных предприятий

Концепция организации хранения данных
Среда хранения — Postgres.
Архитектура данных изначально нацелена на единообразное обращение с данными — создание, редактирование, удаление.
Архитектура должна обспечивать создание «сущностей» на лету. Сущность — это по сути некий справочник в котором элементами являются данные разных классов. Первичный базовый класс сущностей — простой иерархический справочник с обеспечением хранения историзма — т. е. Версий элементов этого справочника.

Ниже перечислены все классы сущностей системы- базовый и порожденные от базового класса сущностей — сущности справочник (указанное действует только в части хранения собственно данных, метаданные имеют отдельную структуру) :
1) Справочник — простой иерархический (опционно) версионный (опционно, но если он не версионный — одна запись в таблице версий присутсвует в любом случае),
2) Документы — справочник, элементы которого включают в себя строки из других справочников- тн подчиненные таблицы, из одного и того же справочника для одного элемента -документа может быть несколько наборов записей — подчиненных таблиц. Они отличаются в пределах одного типа документа ролями. При настройке починенности явно указывается роль подчиненной таблицы в Документе. У строк подчиненных таблиц в свою очередь могут быть свои подчиненные таблицы. Указание в конфигураторе для справочника/документа , что он подчиненный однозначно влечет невозможность отображения этих данных в отрыве от мастер таблицы. В конфигураторе для мастер -таблицы организуется хранение всех подчиненных таблиц(одна подчиненная таблица- одна запись в конфигураторе подчиненности)
Кроме того, документы обладают тем свойством, что они передаются от пользователя к пользователю и от процесса к процессу. При передаче задается действие(алгоритм), задается срок обработки, задается задание, задается роль получателя- из справочника. Между исполнителями организуются доверенные отношения, когда пользователь либо доверяет другому работу вместо себя/вместе с собой с документом, либо когда пользователь делегирует(переназначает) обработку вместо себя — другому лицу (список довереннных и список подчиненных задается)
3) Расчет/алгоритм — простой тип справочника, практически идентичный Справочнику. Хранит ссылки / адреса на диске используемых процедур.
4) Действие/операция — перечень активностей пользователей/процессов в системе. Для действий задается набор проводок (если требуется)
5) Счета — сущность, хранящая обьекты, требующие учета, подсчета, ответсвенного хранения… Например счет — здание котельной. Имеет по умолчанию указание дебетовый он или кредитный.
6) Планы счетов бух учета — различные планы счетов. Все элементы сущностей счета привязываются к счетам бух.учета
7) Ипостаси Счетов — это качественная составляющая хранимых количеств. Например счет Мониторы имеет ипостаси — количество мониторов, стоимость мониторов. При этом стоимость мониторов привязывается в плану счетов, а количество — нет
8) Оргструктуры — справочники со структурами предприятий
 

Сущности физически реализуются в двух таблицах (используются всеми сущностями) и таблицами расширения полей (у каждой сущности своя, именуется EXT_alias — alias задается в мета-таблице описания сущностей)
Также используется таблица, указывающая в какой балансовой единице видна сущность


Ниже приведены описания двух центральных таблиц ядра (схема data), которые обеспечивают жизнь объектов и работу «Машины времени».
1. Таблица data.entity_instances (Ипостаси)
Это реестр «душ» объектов. Здесь фиксируется сам факт существования сущности, её тип и границы владения.
Поле
Тип
Комментарий Амадея
id
serial
Primary Key. Уникальный неизменный ID объекта на всю его жизнь.
entitytypeid
integer
FK к meta.entitytypes. Определяет «породу» объекта (Персона, Накладная и т.д.).
ownerbu_id
integer
FK к auth.balanceunits. (Опционально). Ссылка на БЕ-владельца для жесткой изоляции.
master_instanceid
integer
Self-reference. Указывает на «родителя» для вложенных сущностей (Строка -> Документ).
tdt_birth
timestamp
Дата и время физического создания записи в системе (не меняется никогда).

2. Таблица data.entity_versions (Версии / Машина времени)
Это хроника состояний. Именно здесь хранятся меняющиеся атрибуты (названия) и временные интервалы актуальности.
Поле
Тип
Комментарий Амадея
id
serial
Primary Key. Уникальный ID конкретного «снимка» состояния объекта.
instanceid
integer
FK к data.entity_instances. Привязка версии к конкретной «душе» объекта.
parentinstanceid
integer
FK к data.entity_instances. Используется для построения иерархий (дерево групп/папок).
master_versionid
integer
Self-reference. Ссылка на версию Хозяина. Нужно для синхронного отката Документа со всеми строками.
cname
varchar
Основное наименование объекта (ФИО, Номер документа), актуальное для этой версии.
tdt_start
timestamp
Начало жизни версии. С этого момента данные считаются истинными.
tdt_end
timestamp
Конец жизни версии. По умолчанию 9999-12-31. Если меньше — версия архивная.
cversionstatus
varchar
Текущий статус: Active (текущая), History (прошлое), Draft (черновик).
bfile_data
bytea
(Опционально). Хранилище основного бинарного файла, если объект — это документ/скан.
cfile_name
varchar
Имя и расширение файла, хранящегося в bfile_data.

Аналитическая заметка для твоей документации:
    • Связь с ext: Каждая таблица расширения (например, ext_person) ссылается только на versionid. Это гарантирует, что при поиске «Иванова на прошлый год» система выберет версию из data.entity_versions и автоматически найдет к ней атрибуты (фамилию, ИНН) в ext.
    • Изоляция: entity_instances отвечает за то, КТО видит объект, а entity_versions — за то, ЧТО именно он видит в конкретный момент времени.


3. Таблица data.entity_visibility (Матрица видимости)
Это связующее звено между физическим миром Сущностей и мандатным миром Организаций (БЕ).
Поле
Тип
Комментарий Амадея
id
serial
Primary Key. Уникальный номер записи в матрице.
instanceid
integer
FK к data.entity_instances. Ссылка на «душу» Сущности.
balanceunitid
integer
FK к auth.balanceunits. Конкретная БЕ, которой разрешено видеть эту Сущность.
tdt_granted
timestamp
Время доступа. Когда Сущность стала доступна в этой БЕ.
i_access_level
integer
(Опционально). Уровень прав (0-Только чтение, 1-Полный доступ).

Мета таблицы проектирования сущностей

Это «генетический код» всей системы Mozart, на основе которого Конфигуратор (Level 0) строит физический мир.
1. Таблица meta.entitytypes (Реестр Сущностей)
Здесь задаются «чертежи» объектов: будет это справочник, документ или системный регистр.
Поле
Тип
Описание Амадея
id
serial
PK. Уникальный идентификатор типа сущности.
cname
varchar
Имя. Человеческое название (напр. «Персона»).
calias
varchar
Код. Системный алиас для SQL-запросов (напр. «PERSON»).
cbaseclass
varchar
Класс. Тип поведения: Document, Dictionary, Account.
lisdependent
boolean
Зависимость. true — если сущность не живет сама по себе (строка).
lishierarchy
boolean
Дерево. true — если справочник имеет папки/группы.
lisversioned
boolean
История. true — если Слон ведет историю изменений (Машина времени).
isecuritystrategy
integer
Защита. 0 — нет, 1 — на таблицу, 2 — на каждую запись.





Таблица meta.fields (Реестр Полей)
Твой полный список из 11 полей, который мы «прошили» в IDE.

Поле
Тип
Описание Амадея
id
serial
PK.
entitytypeid
integer
FK. Привязка к сущности.
cfieldname
varchar
SQL Имя. (напр. «clastname»).
calias
varchar
Бизнес-имя. (напр. «Фамилия»).
cfieldtype
char(1)
Венгерка. (C, N, R, G...).
lisrequired
boolean
NOT NULL. Обязательность заполнения.
lisindexed
boolean
Index. Создавать ли индекс в ext_.
nlength
integer
Размер. (100, 18...).
ndecimals
integer
Точность. (0, 2, 4...).
isortorder
integer
Порядок. Позиция в экранной форме.
ref_entitytypeid
integer
Ссылка. На кого ссылаемся, если тип 'R'.

Таблица -список типов полей, допустимых в системе
SELECT ccode, cname, mcomment 	FROM meta.field_types;

"C"	"Строка"	"Текстовые данные фиксированной макс. длины"
"N"	"Число"	"Дробные числа (сумма, количество, вес)"
"I"	"Целое"	"Целочисленные значения (счетчики, ID)"
"D"	"Дата"	"Календарная дата"
"T"	"Дата/Время"	"Точное время события"
"L"	"Логика"	"Да/Нет (Checkbox)"
"M"	"Текст (Memo)"	"Длинные описания, неограниченный текст"
"R"	"Ссылка"	"Связь с другим справочником (Foreign Key)"
"G"	"Картинка"	"Изображение (Blob + Name)"
"V"	"Видео"	"Видеофайл (Blob + Name)"
"F"	"Файл"	"Произвольный документ (Blob + Name)"

meta.field_types (Справочник системных типов)
Эта таблица хранит «Конституцию типов» нашего проекта. Она связывает твою Венгерку (C, N, R...) с понятными названиями для Демиурга.
Поле
Тип
Описание Амадея
ccode
char(1)
Primary Key. Та самая буква (C, N, R, G, V, F, I, D, T, L, M).
cname
varchar(50)
Человеческое имя. То, что видит Демиург в комбобоксе (напр. «Строка», «Ссылка»).
mcomment
text
Описание. Краткая справка о том, как Слон будет хранить эти данные.

Подчиненность таблиц
Таблица meta.composition_roles — это смысловой фильтр для Master-Detail связей. Она объясняет системе, зачем одна сущность вложена в другую. Например, у одной «Накладной» (Master) может быть три вложения типа «Товары» (Child): одно в роли «Основные товары», второе — «Бонусы», третье — «Возвраты».

Таблица meta.composition_roles (Роли вложений)
Эта таблица позволяет Рантайму (Layer 3) разделять данные в интерфейсе (например, выводить их на разные вкладки одного документа).
Поле
Тип
Описание Амадея
id
serial
Primary Key. Уникальный ID роли вложения.
cname
varchar(100)
Имя. Человеческое название (напр. «Спецификация», «Оборудование», «Тара»).
calias
varchar(50)
Код. Системный алиас для программной логики (напр. «ROLE_ITEMS», «ROLE_BONUS»).
mcomment
text
Описание. Заметка о том, как эта роль влияет на бизнес-логику документа.
Пример данных (Партитура по умолчанию):
id
cname
calias
Применение
1
Спецификация
COMP_ITEMS
Основной список ТМЦ в документе.
2
Услуги
COMP_SERVICES
Перечень выполненных работ.
3
Участники
COMP_MEMBERS
Список персон, связанных с документом (напр. комиссия).
4
Файлы
COMP_FILES
Группа прикрепленных сканов или документов.

Связь: В таблице meta.entity_composition поле role_id ссылается именно сюда.
нтерфейс: В нашем Project Manager в нижнем гриде (Зависимые сущности) при добавлении «ребенка» мы будем выбирать одну из этих ролей.
    • Логика: Рантайм может сказать: «Покажи мне все строки Накладной №5, которые имеют роль 'Бонусы'», и Слон мгновенно отфильтрует их через JOIN
Таблица meta.entity_composition (Связи Мастер-Деталь)
Эта таблица описывает вложения. Именно на её основе процедура p_field_sync понимает, что в таблицу «Товары» нужно добавить поле-пуповину (FK), например, invoiceid.
Поле
Тип
Комментарий Амадея
id
serial
PK. Уникальный ID связи вложения.
parent_entityid
integer
Мастер. Ссылка на meta.entitytypes (напр. «Накладная»).
child_entityid
integer
Деталь. Ссылка на meta.entitytypes (напр. «Товары»).
role_id
integer
Роль. Ссылка на meta.composition_roles (напр. «Бонусы»).
c_link_fieldname
varchar(63)
Пуповина. Имя поля в таблице ext_child, которое хранит ID родителя.
isortorder
integer
Порядок. Очередность отображения вкладок в интерфейсе.

🔍 Аналитическая справка Амадея:
    1. Полиморфизм: Одна и та же сущность (например, «Персоны») может быть вложена в разные документы. В «Накладной» она может быть в роли «Грузчики», а в «Приказе» — в роли «Сотрудники».
    2. Генерация кода: Когда ты в IDE добавляешь строку в эту таблицу, Слон:
        ◦ Проверяет, есть ли в таблице ext_child колонка c_link_fieldname.
        ◦ Если нет — делает ALTER TABLE ... ADD COLUMN ... integer.
        ◦ Регистрирует это поле в meta.fields, чтобы ты видел его в общем списке.
    3. Рантайм: Когда Layer 3 открывает «Накладную» с id=500, он смотрит в эту таблицу, видит связь с «Товарами» через поле invoiceid и делает запрос:
SELECT * FROM ext.ext_items WHERE invoiceid = 500.


Объекты системы (в режиме работы)- не являющиеся сущностями
1) Проводки. Имеют две стороны. Привязываются к счетам и их ипостасям

2)регистры  - обеспечивают хранение на определенную дату значений по дебет/кредиту у разных ипостасей счетов

3) связи. Элементы разных сущностей могут быть связаны между собой в рамках какой либо связи. При этом в рамках этой связи и элементов свои роли
Например Связь — Родственные связи. Состоят из экземпляров справочника Персоны, с ролями из справочника — Семейные роли, экземпляров справочника Питомцы  ролями из справочника — Функции питомцев
Экземпляр — родственные связи семьи Ивановых.  Входят Иванов — отец, муж
Иванова -жена, мать,  Иванов — сын, Барбос- собака -поводырь


Групповые (Коллективные) связи — это «социальная сеть» наших сущностей. Именно здесь мы описываем, как «Иванов», «Петров» и «Лабрадор Рекс» объединяются в одну «Семью Ивановых».
Вот детальное описание структуры таблиц для твоей документации: от конфигурации правил (Meta) до физических участников (Data).

1. Метаданные (Конфигуратор)
Здесь  описывается, какие «породы» сущностей могут дружить и из каких справочников им брать роли.
Вот полный список полей для двух ключевых таблиц Графа Связей в схеме meta.
1. Таблица meta.entity_ref_types (Реестр типов связей)
Здесь мы задаем «жанр» отношений (например, «Семейные», «Проектные», «Состав изделия»).
Поле
Тип
Описание Амадея
id
serial
Primary Key. Уникальный ID типа связи.
cname
varchar
Имя. Человеческое название (напр. «Семейные отношения»).
calias
varchar
Код. Системный алиас для логики (напр. «REF_FAMILY»).
mcomment
text
Описание. Твои заметки о том, для чего этот тип связи нужен.

2. Таблица meta.entity_ref_rules (Правила участников)
Это «пропускной пункт»: здесь Демиург решает, кто имеет право войти в связь и где ему выдадут роль.
Поле
Тип
Описание Амадея
id
serial
Primary Key. ID правила.
reftypeid
integer
FK. Ссылка на тип связи из таблицы выше (meta.entity_ref_types).
entitytypeid
integer
Участник. Ссылка на meta.entitytypes (напр. «Персона» или «Животное»).
role_source_entityid
integer
Источник Ролей. Ссылка на сущность-справочник, откуда брать роли для этого участника (напр. «Справочник ролей семьи»).
mcomment
text
Заметка. Комментарий к конкретному правилу (напр. «Только для домашних питомцев»).


Таблица data.entity_linkage_items (Участники связи)
Самая важная таблица — здесь прописаны все «члены семьи».
Поле
Тип
Описание Амадея
id
serial
PK.
linkageid
integer
Ссылка на узел группы (data.entity_linkages).
instanceid
integer
Сущность-участник. Ссылка на data.entity_instances (Иванов).
roleinstanceid
integer
Экземпляр роли. Ссылка на объект роли (Отец, Мать, Поводырь).
tdt_start
timestamp
Когда участник вошел в группу.
tdt_end
timestamp
Когда покинул (по умолчанию 9999-12-31).


Балансовые единицы — это орг единицы имеющие свой законченный контур в ерп

Таблица auth.balanceunits (Реестр БЕ)
Здесь мы описываем «кто в доме хозяин» и выстраиваем структуру холдинга.
Поле
Тип
Комментарий Амадея
id
serial
Primary Key. Уникальный ID БЕ в системе.
parentid
integer
Иерархия. Ссылка на головную БЕ (напр. Филиал -> Головной офис).
cname
varchar
Имя. Официальное название (напр. «Корпорация Жираф»).
calias
varchar
Код. Краткий код для фильтров и поиска (напр. «BU_001»).
inn
varchar(12)
ИНН. Налоговый идентификатор для учета.
kpp
varchar(9)
КПП. Код причины постановки на учет.
is_active
boolean
Статус. true — БЕ работает, false — архив/ликвидирована.
mcomment
text
Заметка. Любые пояснения по этой структуре.




Структура данных
 данные хранятся в следующих схемах 
auth — авторизационные данные, данные о ролях..
data — собственно данные erp
ext — таблицы расширения для сущностей
meta — конфигуратор таблиц

собственно структура данных прилагается далее в файлах


https://aistudio.google.com/prompts/new_chat


