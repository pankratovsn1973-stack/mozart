# -*- coding: utf-8 -*-
# /home/sergey/Documents/configurate/widgets/form_designer_old/README_designer.py
"""
===============================================================================
ВИЗУАЛЬНЫЙ ДИЗАЙНЕР ФОРМ MOZART (form_designer_old)
===============================================================================

Данный файл содержит документацию по всем модулям визуального дизайнера форм.
Дизайнер позволяет создавать и редактировать формы перетаскиванием контролов,
настраивать их свойства, привязывать методы и события.

ОСНОВНЫЕ ПРИНЦИПЫ:
- WYSIWYG-редактор с поддержкой Drag & Drop
- Сетка с привязкой (snap) для точного позиционирования
- Инспектор свойств для настройки контролов
- Поддержка методов и событий
- Сохранение форм в БД (meta.forms, meta.form_elements)

===============================================================================
АРХИТЕКТУРА (МОДЕЛЬ-СЦЕНА-ВИД)
===============================================================================

1. FormDesigner (main_widget.py) - главный виджет
2. FormDesignerScene (form_scene.py) - сцена
3. FormDesignerView (form_view.py) - вьювер
4. FormBackground (form_background.py) - бланк формы
5. ControlItem (control_item.py) - графическое представление контрола
6. ResizeHandle (resize_handle.py) - маркеры изменения размера

===============================================================================
МОДУЛИ (по порядку)
===============================================================================
"""

# -----------------------------------------------------------------------------
# 1. main_widget.py - FormDesigner (главный виджет)
# -----------------------------------------------------------------------------
"""
Наследник: QWidget
Назначение: Главный виджет дизайнера, объединяющий все компоненты

Компоненты:
    - PaletteWidget (слева) - палитра контролов для перетаскивания
    - FormDesignerScene + FormDesignerView (центр) - рабочая область
    - PropertyEditor (справа) - инспектор свойств
    - MethodsPanel (справа, вкладка) - методы
    - EventsPanel (справа, вкладка) - события
    - HierarchyWidget (слева, снизу) - дерево иерархии контролов

Методы:
    - init_form(width, height, title) - создать новый бланк формы
    - load_form() - загрузить форму из БД
    - save_form() - сохранить форму в БД
    - clear_form() - очистить все контролы
    - toggle_grid() - показать/скрыть сетку
    - toggle_snap() - включить/отключить привязку к сетке
    - change_grid_size(size) - изменить шаг сетки

Сигналы:
    - form_changed() - форма изменена

Интеграция с БД:
    - Читает meta.forms (mstate_json)
    - Сохраняет в meta.form_elements
"""

# -----------------------------------------------------------------------------
# 2. form_scene.py - FormDesignerScene (сцена)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsScene
Назначение: Сцена, на которой размещаются бланк формы и контролы

Компоненты сцены:
    - background: FormBackground (бланк формы)
    - controls: dict {control_id: ControlItem}
    - resize_handles: list (маркеры для формы)
    - control_handles: list (маркеры для контрола)

Методы:
    - init_form(width, height, title) - создать бланк формы
    - add_control(widget, control_id, control_type, x, y) - добавить контрол
    - delete_control(control_id) - удалить контрол
    - clear_selection() - снять выделение
    - get_controls_data() - получить данные всех контролов для сохранения
    - set_grid_visible(visible) - показать/скрыть сетку
    - set_grid_size(size) - изменить шаг сетки
    - set_snap_enabled(enabled) - включить/отключить привязку

События:
    - dragEnterEvent, dragMoveEvent, dropEvent - приём контролов из палитры
    - drawBackground - рисование сетки
    - mousePressEvent, mouseMoveEvent, mouseReleaseEvent - работа с мышью
    - keyPressEvent - удаление контролов (Delete)

Сигналы:
    - control_selected(control_item) - выбран контрол
    - form_selected() - выбрана форма
    - form_resized(width, height) - изменён размер формы
    - geometry_changed(id, x, y, width, height) - изменена геометрия

Особенности:
    - Привязка к сетке (snap) для точного позиционирования
    - Выделение контролов (пунктирная рамка)
    - Групповое перемещение (Ctrl+клик)
"""

# -----------------------------------------------------------------------------
# 3. form_view.py - FormDesignerView (вьювер)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsView
Назначение: Вьювер для отображения сцены

Особенности:
    - Масштабирование колесиком мыши (Ctrl + колёсико)
    - Поддержка RubberBandDrag (выделение рамкой)
    - Сброс масштаба через reset_zoom()

Методы:
    - reset_zoom() - сбросить масштаб к 1.0
    - wheelEvent(event) - масштабирование

Параметры:
    - setRenderHint(QPainter.Antialiasing) - сглаживание
    - setDragMode(QGraphicsView.RubberBandDrag) - выделение рамкой
    - setTransformationAnchor(AnchorUnderMouse) - масштабирование под курсором
"""

# -----------------------------------------------------------------------------
# 4. form_background.py - FormBackground (бланк формы)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsRectItem
Назначение: Визуальное представление бланка формы на сцене

Компоненты:
    - title_bar: TitleBar - заголовок с кнопками
    - tool_bar: ToolBarBar - панель инструментов
    - status_bar: StatusBarBar - статусная строка
    - main_menu: QMenuBar - главное меню (для типа main_interface)

Свойства:
    - width, height - размеры формы
    - title - заголовок
    - form_type - тип формы: "edit", "main_interface"
    - interface_role - роль, чьё меню отображать

Методы:
    - set_size(width, height) - изменить размер
    - set_title(title) - изменить заголовок
    - update_layout() - обновить расположение заголовка, тулбара, статусбара

Особенности:
    - При изменении размера корректирует позиции дочерних контролов
    - Для типа main_interface добавляет QMenuBar
"""

# -----------------------------------------------------------------------------
# 5. control_item.py - ControlItem (графический объект контрола)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsProxyWidget
Назначение: Оборачивает реальный виджет контрола для размещения на сцене

Свойства:
    - control_widget: QWidget - реальный виджет
    - control_id: str - уникальный идентификатор
    - control_type: str - тип контрола (textbox, button и т.д.)
    - is_selected: bool - выделен ли

Методы:
    - set_selected(selected) - выделить/снять выделение
    - _get_widget_size() - безопасно получить размеры виджета
    - _emit_geometry() - испустить сигнал об изменении геометрии
    - get_data() - получить данные для сохранения
    - update_property(prop_name, value) - обновить свойство
    - update_geometry_from_properties() - обновить позицию из свойств

События:
    - contextMenuEvent - контекстное меню (Удалить, Свойства)
    - mousePressEvent, mouseMoveEvent, mouseReleaseEvent - перемещение
    - paint - отрисовка рамки выделения

Фильтр событий:
    - DesignEventFilter - блокирует клики внутри виджета в режиме дизайна

Особенности:
    - Поддерживает групповое выделение (Ctrl + клик)
    - При перемещении учитывает границы формы
    - Привязка к сетке (через scene.snap_enabled)
"""

# -----------------------------------------------------------------------------
# 6. resize_handle.py - ResizeHandle (маркер изменения размера)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsRectItem
Назначение: Маркеры для изменения размера формы или контрола

Позиции (POSITIONS):
    - 'tl', 'tr', 'bl', 'br' - углы (диагональное изменение)
    - 't', 'b' - верх, низ (вертикальное изменение)
    - 'l', 'r' - лево, право (горизонтальное изменение)

Методы:
    - update_position() - обновить позицию маркера относительно целевого объекта
    - _get_target_size() - безопасно получить размеры целевого объекта
    - mousePressEvent, mouseMoveEvent, mouseReleaseEvent - изменение размера

Особенности:
    - Работает как с ControlItem, так и с FormBackground
    - Учитывает минимальные размеры (min_w, min_h)
    - При изменении размера контрола вызывает _emit_geometry()
    - Привязка к сетке через scene.snap_point()
"""

# -----------------------------------------------------------------------------
# 7. palette_widget.py - PaletteWidget (палитра контролов)
# -----------------------------------------------------------------------------
"""
Наследник: QWidget
Назначение: Палитра контролов для перетаскивания на форму

Компоненты:
    - QScrollArea с вертикальным layout
    - DraggableLabel для каждого типа контрола

Класс DraggableLabel (внутренний):
    - Наследник: QLabel
    - При mousePressEvent создаёт QDrag с QMimeData
    - MIME-тип: "text/plain" с текстом control_type

Список контролов в палитре:
    ('textbox', '📝 Текстовое поле'),
    ('numberbox', '#️⃣ Числовое поле'),
    ('datebox', '📅 Дата'),
    ('checkbox', '☑ Флажок'),
    ('optiongroup', '🔘 Группа радиокнопок'),
    ('memo', '📄 Многострочный текст'),
    ('reference', '🔗 Ссылка'),
    ('button', '🔘 Кнопка'),
    ('label', '🏷 Метка'),
    ('group', '🗋 Группа'),
    ('grid', '▦ Таблица'),

Особенности:
    - При перетаскивании курсор меняется на OpenHandCursor
"""

# -----------------------------------------------------------------------------
# 8. property_editor.py - PropertyEditor (инспектор свойств)
# -----------------------------------------------------------------------------
"""
Наследник: QWidget
Назначение: Инспектор свойств выбранного объекта (формы или контрола)

Компоненты:
    - QTableWidget (2 колонки: свойство, значение)
    - Кнопка "Добавить свойство"

Методы:
    - set_control(control_item) - показать свойства контрола
    - set_form(form_object) - показать свойства формы
    - load_properties() - загрузить все свойства в таблицу
    - _add_property_row(display_name, value, prop_type, internal_name)
    - edit_property(item) - редактирование свойства (диалог)
    - add_property() - добавить пользовательское свойство

Сигналы:
    - property_changed(prop_name, value)

Использует PropertyInspector.inspect() для получения свойств объекта

Особенности:
    - Поддерживает типы: str, bool, int, float
    - Для bool - QCheckBox в диалоге
    - Для int/float - QInputDialog
"""

# -----------------------------------------------------------------------------
# 9. property_inspector.py - PropertyInspector (сервис инспекции)
# -----------------------------------------------------------------------------
"""
Класс: PropertyInspector (статический)
Назначение: Сканирует объект и возвращает все его свойства

Метод:
    - inspect(obj) -> dict

Возвращает словарь:
    {technical_name: (display_name, current_value, value_type)}

Что сканирует:
    - Нативные свойства Qt (через metaObject)
    - Python-переменные инстанса, начинающиеся с '_' (но не с '__')

Фильтрует:
    - Свойства с именами: geometry, pos, size, windowIcon, palette, font и т.д.
    - Сигналы (имеющие emit)
"""

# -----------------------------------------------------------------------------
# 10. methods_panel.py - MethodsPanel (панель методов)
# -----------------------------------------------------------------------------
"""
Наследник: QWidget
Назначение: Управление методами (скриптами) формы или контрола

Компоненты:
    - QListWidget - список методов
    - QPushButton "Добавить"

Методы:
    - set_control(control_item, calc_code) - показать методы контрола
    - _parse_methods_from_code(calc_code) - извлечь методы из кода

Сигналы:
    - edit_method_requested(method_name, code)
    - add_method_requested()

Поиск методов:
    - Регулярное выражение: def {alias}_{method_name}(

Особенности:
    - Методы должны иметь префикс с алиасом объекта
    - При двойном клике открывается CodeEditorDialog
"""

# -----------------------------------------------------------------------------
# 11. events_panel.py - EventsPanel (панель событий)
# -----------------------------------------------------------------------------
"""
Наследник: QWidget
Назначение: Привязка событий контрола к методам

Компоненты:
    - QTableWidget (событие, метод)

Стандартные события:
    - on_click, on_dbl_click, on_change, on_focus, on_blur
    - on_key_press, on_mouse_enter, on_mouse_leave

Методы:
    - set_control(control_item, saved_events=None)
    - set_available_methods(methods)
    - save_events()
    - get_events()

Особенности:
    - Список доступных методов берётся из MethodsPanel
"""

# -----------------------------------------------------------------------------
# 12. code_editor.py - CodeEditorDialog (диалог редактирования кода)
# -----------------------------------------------------------------------------
"""
Наследник: QDialog
Назначение: Редактирование кода метода

Компоненты:
    - QPlainTextEdit с моноширинным шрифтом
    - Кнопки "Сохранить", "Отмена"

Методы:
    - get_code() -> str

Особенности:
    - Размер диалога 800x500
    - Шрифт Courier New, 10pt
"""

# -----------------------------------------------------------------------------
# 13. hierarchy_widget.py - HierarchyWidget (дерево иерархии)
# -----------------------------------------------------------------------------
"""
Наследник: QWidget
Назначение: Дерево контролов формы (удобно для навигации)

Компоненты:
    - QTreeWidget

Методы:
    - load_controls(controls_data) - загрузить иерархию
    - select_item(control_id) - выделить элемент в дереве
    - clear_selection() - снять выделение
    - clear() - очистить дерево

Сигналы:
    - item_selected(control_id)

Особенности:
    - Строит иерархию на основе parent_id
"""

# -----------------------------------------------------------------------------
# 14. grid_manager.py - GridManager (управление сеткой)
# -----------------------------------------------------------------------------
"""
Класс: GridManager
Назначение: Управление координатной сеткой

Свойства:
    - grid_size: int (шаг сетки)
    - snap_enabled: bool (привязка)
    - visible: bool (видимость)

Методы:
    - snap(point, origin) - привязать точку к сетке
    - draw(painter, rect, origin) - нарисовать сетку
    - toggle_visible() - переключить видимость
    - toggle_snap() - переключить привязку

Особенности:
    - Размер сетки ограничен 4-100 пикселей
    - При маленьком размере рисует точки, при большом - линии
"""

# -----------------------------------------------------------------------------
# 15. title_bar.py - TitleBar (заголовок формы)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsWidget
Назначение: Заголовок формы с кнопками и возможностью перетаскивания

Компоненты:
    - TitleText (обёртка для QGraphicsTextItem)
    - LayoutableButton (минимальная, закрытия)

Класс LayoutableButton (внутренний):
    - Наследник: QGraphicsWidget
    - Поддержка hover-эффектов
    - Сигналы clicked

Методы:
    - set_title(title)
    - get_title()

Сигналы:
    - minimize_clicked, close_clicked

Особенности:
    - В режиме дизайна кнопки неактивны
    - Позволяет перетаскивать форму
"""

# -----------------------------------------------------------------------------
# 16. toolbar_bar.py - ToolBarBar (панель инструментов)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsWidget
Назначение: Панель инструментов формы

Компоненты:
    - ToolBarButton для каждой команды

Класс ToolBarButton (внутренний):
    - Поддержка checkable (фиксации)
    - Сигналы clicked(symbol), toggled(bool)

Кнопки тулбара:
    - 💾 Сохранить
    - ✂️ Вырезать
    - 📋 Копировать
    - 📌 Вставить
    - ⬅️ Выровнять по левому краю
    - ⬌ Выровнять по центру
    - ➡️ Выровнять по правому краю

Сигналы:
    - save_clicked, cut_clicked, copy_clicked, paste_clicked
    - align_left_clicked, align_center_clicked, align_right_clicked
"""

# -----------------------------------------------------------------------------
# 17. statusbar_bar.py - StatusBarBar (статусная строка)
# -----------------------------------------------------------------------------
"""
Наследник: QGraphicsWidget
Назначение: Статусная строка формы

Отображает:
    - Координаты мыши относительно формы
    - Текущий масштаб в процентах
    - Режим (проектирование/выполнение)
    - Информационные сообщения

Методы:
    - set_coordinates(x, y)
    - set_zoom(zoom_percent)
    - set_design_mode(design_mode)
    - set_status(text)
    - show_message(text)

Особенности:
    - В режиме проектирования подсвечивает масштаб:
        - зелёный = 100%
        - синий < 100%
        - красный > 100%
"""

# -----------------------------------------------------------------------------
# 18. visual_form_editor.py - обёртка (для обратной совместимости)
# -----------------------------------------------------------------------------
"""
Назначение: Обёртка для импорта FormDesigner из старого расположения

Содержит:
    from .form_designer_old.main_widget import FormDesigner as VisualFormEditor

Используется в:
    - widgets/visual_form_editor.py
    - tabs/forms/form_properties_dialog.py
"""

# -----------------------------------------------------------------------------
# ПРИМЕР ИСПОЛЬЗОВАНИЯ ДИЗАЙНЕРА
# -----------------------------------------------------------------------------
"""
# Создание дизайнера
from widgets.form_designer_old import FormDesigner
designer = FormDesigner(db=db_service, form_id=123)

# Создание новой формы
designer.init_form(width=800, height=600, title="Новая форма")

# Загрузка существующей формы
designer.set_form_id(456)
designer.load_form()

# Сохранение
designer.save_form()

# Управление сеткой
designer.toggle_grid()
designer.change_grid_size("15 px")

# Получение данных формы
form_data = designer.get_form_data()
"""

# -----------------------------------------------------------------------------
# СВЯЗЬ С БАЗОЙ ДАННЫХ
# -----------------------------------------------------------------------------
"""
Таблицы:
    - meta.forms
        - id: PK
        - calias: алиас формы
        - cname: имя формы
        - ientitytypeid: FK на meta.entitytypes
        - mstate_json: JSON с состоянием формы

    - meta.form_elements
        - id: PK
        - formid: FK на meta.forms
        - parentid: иерархия
        - calias: алиас контрола
        - cclass: тип контрола
        - mproperties_json: JSON с настройками

Пример mstate_json:
{
    "form": {
        "width": 800,
        "height": 600,
        "title": "Форма",
        "form_type": "edit"
    }
}

Пример mproperties_json для контрола:
{
    "binding_field": "cname",
    "label": "Имя",
    "x": 10, "y": 50,
    "width": 200, "height": 30
}
"""

# -----------------------------------------------------------------------------
# ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ
# -----------------------------------------------------------------------------
"""
1. Нет поддержки вложенных контейнеров (нельзя перетащить контрол внутрь группы)
2. Нет Undo/Redo
3. OptionGroup не позволяет перемещать отдельные радиокнопки в дизайнере
4. Reference не позволяет перемещать кнопки отдельно
5. Нет экспорта/импорта из .ui файлов (только через Qt Designer отдельно)
6. Методы и события сохраняются в БД, но не генерируются в отдельные файлы
"""

# -----------------------------------------------------------------------------
# ПЛАНИРУЕМЫЕ УЛУЧШЕНИЯ
# -----------------------------------------------------------------------------
"""
1. Поддержка вложенных контейнеров
2. Undo/Redo
3. Поддержка перемещения дочерних элементов OptionGroup и Reference
4. Экспорт/импорт .ui файлов
5. Генерация Python-кода для форм
6. Поддержка пользовательских контролов
7. Мастер создания форм (4 типа)
"""